{
  "name": "gamedev-toolkit",
  "version": "1.0.0",
  "private": true,
  "description": "Game development toolkit workspace",
  "packageManager": "pnpm@12.4.2",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "typecheck": "pnpm -r typecheck",
    "config-export": "node tools/config-export/dist/index.js",
    "font-subset": "node tools/font-subset/dist/index.js"
  },
  "devDependencies": {
    "@types/node": "^20.17.0",
    "typescript": "^5.6.3"
  }
}
